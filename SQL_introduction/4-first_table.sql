-- Task: Create the table first_table if it doesn't exist
-- Query: CREATE TABLE IF NOT EXISTS first_table (
--            id INT,
--            name VARCHAR(256)
--        );
CREATE TABLE
IF NOT EXISTS first_table
(
    id INT,
    name VARCHAR
(256)
);
